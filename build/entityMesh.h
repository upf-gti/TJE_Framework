#include "framework/entities/entity.h"
#include "framework/camera.h"
/*
class Mesh;
class Shader;
class Texture;
class EntityMesh : public Entity { 

public:

	// Attributes of the derived class  
	Mesh* mesh;
	Texture* texture;
	Material* material; //Texture to be painted on the mesh
	Shader* shader;
	Vector4 color;

	// Methods overwritten from base class
	EntityMesh(Mesh* me, Texture* te, Material* ma, Shader* s, Vector4 c);
	void render();
	void update(float elapsed_time);
};

*/